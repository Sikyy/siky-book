import SwiftUI

// NOTE: ModelContainer(.modelContainer(for: [Book.self, Chapter.self, BookSource.self]))
// will be added in Task 2 once the SwiftData models are defined.
@main
struct NovelReaderApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
